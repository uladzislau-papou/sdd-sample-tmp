Based on the file `plan.md` in the project root,
create a detailed plan in `task.md`.
Break down the tasks incrementally and small.
Make a checkmark before each task "- [ ]" so that you can check it off in the future.
Create only the file `task.md`. Don't modify any files yet or start implementing.

Number the task blocks like this:

```markdown
## Phase 1: Foundation Molecules

### 1.1 - M1 - Card
- [ ] **Task 1.1**: Implementiere `src/lib/ui/molecules/Card.svelte` (Props: variant, padding, as; Slots: header, default, footer).
- [ ] **Task 1.2**: Füge Export in `src/lib/ui/molecules/index.ts` hinzu.
- [ ] **Task 1.3**: Erstelle Sektion "Card" in `/dev/molecules` mit allen Varianten und Paddings (verwende `projectMock` für Inhalte).
- [ ] **Task 1.4**: Führe `npm run check` durch.
  hier also Phase 1 und der Task-Block 1.1 dann so. Damit kann ich auf den Task-Block referenzieren.
```
Where Phase 1 and task block 1.1 are one piece of imeplementation you can in one iteration.

Wait until I explicitly ask you to do something.