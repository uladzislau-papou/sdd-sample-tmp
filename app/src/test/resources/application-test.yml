spring:
  datasource:
    url: jdbc:h2:mem:alpine_booking_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    enabled: true
    locations: classpath:db/migration
  jooq:
    sql-dialect: H2
