spring:
  datasource:
    url: jdbc:h2:file:./data/alpine-booking;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    enabled: true
    locations: classpath:db/migration
  jooq:
    sql-dialect: H2
