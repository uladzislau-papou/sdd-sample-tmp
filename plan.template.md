# Plan — <effort name>

<!--
The working plan for one effort: a feature, a migration, a use case, a piece of
groundwork. `/uc-to-plan` writes this from a use-case spec; a larger effort gets one by
hand.

`plan.md` and `tasks.md` are a pair. This file holds the reasoning; `tasks.md` holds the
checkboxes derived from it. When they disagree, this file wins and `tasks.md` is a
scoreboard.

Both are working artifacts. Neither is authoritative over `documentation/` — see the
authority order in `CLAUDE.md`.

Delete these comments as you fill it in.
-->

## What this is

<!-- Two or three sentences. What is being built or changed, and what exists today.
     Written so somebody picking this up after two weeks away does not have to reconstruct
     it. -->

## Definition of Done for this plan

<!-- The exit condition, as checkable items. Each one objectively verifiable: a named
     passing test, an existing file, a PASS verdict, a green gate.

     If the plan implements a use case, these mirror the spec's § 10 and the spec wins. -->

- [ ]
- [ ]

## Phases

<!-- Ordered. State *why* the order is what it is wherever it is not obvious — an order
     with a reason survives being interrupted; an order without one gets rearranged by
     whoever resumes.

     A phase that must come first for a non-obvious reason should say so loudly. -->

### Phase 0 — <name>

<!-- If any step is irreversible or reaches outside this repository, it belongs first, on
     its own, before anything else is touched. -->

### Phase 1 — <name>

## Risks

<!-- What could make this plan wrong, rather than what could make it fail.

     Include anything you decided *against* your own recommendation, and say so plainly —
     otherwise a later reader mistakes it for an oversight and quietly "fixes" it.

     Include what will remain unverified when the plan is complete, and why. An unverified
     part that nobody wrote down reads as verified. -->
