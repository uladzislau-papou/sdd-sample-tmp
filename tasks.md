# Tasks — <effort name>

<!--
Derived from `plan.md`. `/plan-to-task` writes this.

The plan is authoritative; this is a scoreboard plus a record. Two rules earn their keep:

  - **A box is ticked only on witnessed evidence.** Not on a predicted test result, not on
    a review verdict you expect. `test.definition.md` § 9 requires an item to name the test
    that satisfies it — and `SpecCitationsTest` checks that the named test exists, so an
    invented citation fails the build rather than sitting there looking complete.
  - **Deviations are recorded, not silently absorbed.** When reality forces a different
    route than the plan, write down what changed and why. That section is usually the most
    valuable thing in this file, and it is the part that is always tempting to skip.

Delete these comments as you fill it in.
-->

## DoD Scoreboard — Plan

<!-- Mirrored from plan.md. The plan wins. -->

- [ ]
- [ ]

## Phase 0 — <name>

- [ ] **0.1**
- [ ] **0.2**

## Phase 1 — <name>

- [ ] **1.1**

---

## Deviations from `plan.md`

<!-- Append as they happen, not at the end — at the end you will remember the decision and
     not the reason.

     One short section each: what the plan said, what actually happened, and why the
     difference was necessary. Include the ones where you were wrong, and the ones where a
     gate caught you. Those are the entries a later reader learns from.

     `[~]` is a useful third checkbox state for a task that was completed differently, or
     partially, with the difference explained here. A task silently left unticked tells the
     next reader nothing. -->
